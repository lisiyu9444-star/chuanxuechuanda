# 天命穿搭 - 设计指南

## 品牌定位

- **应用名称**：天命穿搭
- **定位**：基于八字命理的每日穿搭推荐工具
- **风格**：东方玄学 × 轻奢质感，神秘优雅
- **目标用户**：对传统文化感兴趣的年轻群体（20-35岁）

## 配色方案

### 主色板（全局主题，与 app.css 变量一致）
- **主色**：`#0f172a`（slate-900 深藏青黑，同首页底部主按钮）→ 按钮/图标/强调统一使用 `bg-slate-900`、`text-slate-900` 或主题变量 `bg-primary` / `text-primary`
- **背景色**：`#ffffff`（页面）、`#f9fafb`（gray-50 页面区块底）
- **卡片底色**：`#ffffff`，浅色分组底 `bg-gray-50`
- **辅助底**：`#f1f5f9`（slate-100，对应 secondary 变量）

### 文字色
- **主文字**：`#111827`（gray-900）→ `text-gray-900`
- **次要文字**：`#6b7280`（gray-500）→ `text-gray-500`
- **弱提示**：`#9ca3af`（gray-400）→ `text-gray-400`

### 五行元素色（业务内容色，唯一来源 `@/constants/element-colors`）
- 木：`#22c55e` | 火：`#ef4444` | 土：`#6b4c7a` | 金：`#fbbf24` | 水：`#3b82f6`
- 幸运色/用神徽章的展示色属**内容色**（由 AI 推荐数据驱动），允许出现紫等业务色彩

### 边框与分割
- **边框**：`#e5e7eb`（gray-200）→ `border-gray-200`
- **分割线**：`#f3f4f6`（gray-100）

> **禁令**：全局 UI 主题色禁止紫色系（#7C3AED / #8b5cf6 / #a855f7 等）。新增页面的按钮、图标、选中态、进度条、焦点环一律使用 slate-900 主色；仅业务内容色（幸运色展示）例外。

## 字体规范

- 标题使用 `font-serif`，传递古典气质
- 正文使用默认无衬线体
- 装饰性天干地支文字使用 `font-mono`

## 间距系统

- 页面边距：`px-6`
- 卡片内边距：`p-5` 或 `p-6`
- 组件间距：`gap-4` 或 `gap-6`
- 区块间距：`space-y-8`

## 组件使用原则

- 通用 UI 组件（按钮、输入框、弹窗、卡片等）优先使用 `@/components/ui/*`
- 按钮使用 `Button` 组件，通过 className 自定义金色主题
- 输入框使用 `Input` 组件，深色主题适配
- 卡片使用 `Card` 组件，深色底色
- 选择器使用 `RadioGroup` 组件（性别选择）
- 进度展示使用 `Progress` 组件
- 新页面开发前先拆分 UI 单元，映射到组件库

## 导航结构

- 无 TabBar（单流程应用：首页 → 计算页 → 结果页）
- 页面跳转使用 `navigateTo`
- 导航栏背景色：`#0d1117`，文字颜色：白色

## 容器样式

- 卡片：`bg-[#161b22] rounded-lg border border-[#2a2a35]`
- 输入区域：`bg-[#1a1f28] rounded-lg`
- 按钮：金色主题 `bg-[#c9a96e] text-[#0d1117]`

## 小程序约束

- 包体积控制在 2MB 以内
- 图片全部使用远程 URL（TOS 存储）
- 避免大量 CSS 动画，控制渲染性能
