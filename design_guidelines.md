# 天命穿搭 - 设计指南

## 品牌定位

- **应用名称**：天命穿搭
- **定位**：基于八字命理的每日穿搭推荐工具
- **风格**：东方玄学 × 轻奢质感，神秘优雅
- **目标用户**：对传统文化感兴趣的年轻群体（20-35岁）

## 配色方案

### 主色板
- **背景色**：`#0d1117`（深夜底色）→ 使用 `bg-[#0d1117]`
- **卡片底色**：`#161b22` → 使用 `bg-[#161b22]`
- **主金色**：`#c9a96e`（品牌色）→ 使用 `text-[#c9a96e]`、`bg-[#c9a96e]`
- **辅助紫**：`#8b7ec8` → 使用 `text-[#8b7ec8]`

### 文字色
- **主文字**：`#f0ebe3`（暖白）→ 使用 `text-[#f0ebe3]`
- **次要文字**：`#8b8680`（旧墨灰）→ 使用 `text-[#8b8680]`

### 五行元素色
- 木：`#4a9e6e` | 火：`#c75450` | 土：`#c9a054` | 金：`#b8b0a8` | 水：`#4a7fb5`

### 边框与分割
- **边框**：`#2a2a35` → 使用 `border-[#2a2a35]`
- **分割线**：`#1e1e28`

## 字体规范

- 标题使用 `font-serif`，传递古典气质
- 正文使用默认无衬线体
- 装饰性天干地支文字使用 `font-mono`

## 间距系统

- 页面边距：`px-6`
- 卡片内边距：`p-5` 或 `p-6`
- 组件间距：`gap-4` 或 `gap-6`
- 区块间距：`space-y-8`

## 组件使用原则

- 通用 UI 组件（按钮、输入框、弹窗、卡片等）优先使用 `@/components/ui/*`
- 按钮使用 `Button` 组件，通过 className 自定义金色主题
- 输入框使用 `Input` 组件，深色主题适配
- 卡片使用 `Card` 组件，深色底色
- 选择器使用 `RadioGroup` 组件（性别选择）
- 进度展示使用 `Progress` 组件
- 新页面开发前先拆分 UI 单元，映射到组件库

## 导航结构

- 无 TabBar（单流程应用：首页 → 计算页 → 结果页）
- 页面跳转使用 `navigateTo`
- 导航栏背景色：`#0d1117`，文字颜色：白色

## 容器样式

- 卡片：`bg-[#161b22] rounded-lg border border-[#2a2a35]`
- 输入区域：`bg-[#1a1f28] rounded-lg`
- 按钮：金色主题 `bg-[#c9a96e] text-[#0d1117]`

## 小程序约束

- 包体积控制在 2MB 以内
- 图片全部使用远程 URL（TOS 存储）
- 避免大量 CSS 动画，控制渲染性能
